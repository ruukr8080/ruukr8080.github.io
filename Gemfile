# frozen_string_literal: true

source "https://rubygems.org"

#제거대상

gem 'rouge'
gem 'sass-embedded', '~> 1.77', '>= 1.77.5'
gem 'csv' # (Comma-Separated Values) 를 사용할 수 있도록 도와주는 RubyGem입니다. CSV는 텍스트 파일 형식으로 데이터를 저장할 때 주로 사용되며
group :jekyll_plugins do
# - jekyll-optional-front-matter
# - jekyll-readme-index
# - jekyll-last-modified-at
#   gem "jekyll-mentions" #블로그 포스트에서 GitHub 사용자 멘션 기능을 추가해줍니다.
#   gem "jekyll-toc" #포스트의 목차(Table of Contents)를 자동으로 생성해줍니다.
#   gem 'jekyll-compose' # HTML 파일을 압축하여 공백을 제거하고 줄바꿈을 최소화하여 파일 크기를 줄이는 역할을 합
    gem 'jekyll-avatar'
    gem 'jekyll-default-layout' #기본 레이아웃을 설정해줌
    gem 'jekyll-feed' # 지킬 게시물의 Atom 피드를 생성하기 위한 지킬 플러그인
    gem 'jekyll-paginate'
    gem 'jekyll-redirect-from'
    gem 'jekyll-relative-links'
    gem 'jekyll-seo-tag'
    gem 'jekyll-sitemap'
    gem 'jekyll-titles-from-headings'
    gem 'jekyll-include-cache'
    gem 'jemoji'
    gem 'jekyll-sass-converter'
end

group :test do
  # gem 'html-proofer', '~> 5.0'
end